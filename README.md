# cuda-reduction-shared-memory
A CUDA C++ practice project that sums an integer array using parallel reduction. It demonstrates GPU memory allocation, data transfer, shared memory, thread synchronization, and result verification by comparing the GPU output with a CPU-calculated sum.
